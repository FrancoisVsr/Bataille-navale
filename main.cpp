#include <iostream>
#include "game.h"

int main() {
    start();
    int choix_mode = menu();
    if(choix_mode == 2) {
        
    }
    else if(choix_mode == 1) {
        
    }
    
    return 0;
}